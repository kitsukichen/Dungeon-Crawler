using UnityEngine;

// be able to put item controller onto player
// connect player to coin count ui
// when interacting with coin it will add that item to this script

public class ItemController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
